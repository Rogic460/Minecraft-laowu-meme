# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512M
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# 1.21/1.21.1 共用分支（经典管线）。JDK 25 仍可作 gradle JVM（options.release=21 交叉编译到 21）。
# 关键事实：Minecraft 没有 "1.21.0" 版本号，1.21 系列初版 id 就是 "1.21"（.0 隐含）。
#   所以 minecraft_version 必须写 1.21（不是 1.21.0，否则 loom 在清单里找不到而报 "Failed to find minecraft version"）。
#   人类可读的 jar 标签仍用 +1.21.0（见 build.gradle remapJar）。
# 映射基线选 1.21：fabric-api 下限 0.102.0，1.21.1（api 0.116.12）天然满足，一个 jar 通吃两版。
# loom 1.13.6：与 26.x / 1.21.11 同款。支持 MC 1.21，且内置 tiny-remapper 在 JDK 25 上不卡（1.12.4 会死锁）。
#   （注：build.gradle buildscript 的 loom 版本是硬编码的，gradle.properties 的 loom_version 仅作记录，不被读取。）

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.21
loader_version=0.16.10
loom_version=1.13.6

# Mod Properties
mod_version=1.2.1
maven_group=com.rogic

# Dependencies
fabric_api_version=0.102.0+1.21
modmenu_version=11.0.0
